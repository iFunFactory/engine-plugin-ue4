<?xml version="1.0" encoding="utf-8"?>
<!-- steps to add to build additions -->
<root xmlns:android="http://schemas.android.com/apk/res/android">
	<!-- init section is always evaluated once per architecture -->
	<init>
	    <setBool result="bSupported" value="false"/>
	        <isArch arch="armeabi-v7a">
	            <setBool result="bSupported" value="true"/>
	        </isArch>
			<isArch arch="arm64-v8a">
		         <setBool result="bSupported" value="true"/>
		    </isArch>
	</init>

	<!-- optional files or directories to copy to Intermediate/Android/APK -->
	<resourceCopies>
	    <isArch arch="armeabi-v7a">
		    <!-- src : set relate path from fun_APL.xml to .so file -->
	        <copyFile src="$S(PluginDir)/ARMv7/libopenssl_wrapper.so"
	                  dst="$S(BuildDir)/libs/armeabi-v7a/libopenssl_wrapper.so"/>
		</isArch>
		 <isArch arch="arm64-v8a">
		     <!-- src : set relate path from fun_APL.xml to .so file -->
		     <copyFile src="$S(PluginDir)/ARM64/libopenssl_wrapper.so"
	                  dst="$S(BuildDir)/libs/arm64-v8a/libopenssl_wrapper.so"/>
		 </isArch>
	</resourceCopies>

	<!-- optional libraries to load in GameActivity.java before libUE4.so -->
	<soLoadLibrary>
	    <if condition="bSupported">
	        <true>
		    <loadLibrary name="openssl_wrapper" failmsg="Failed to load openssl_wrapper library"/>
		</true>
	    </if>
	</soLoadLibrary>
</root>